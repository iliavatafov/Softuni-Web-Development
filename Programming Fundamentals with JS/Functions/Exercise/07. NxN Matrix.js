function NxNMatrix(num) {

    for (let i = 0; i < num; i++) {
        let temp = ``;
        for (let j = 0; j < num; j++) {
            temp += num + ` `;
        }
        console.log(temp);
    }
    
}
